import Link from "next/link";import {Navbar,Footer,PageHero,Breadcrumb} from "../components/site";import {schemeList} from "../components/data";
export default function Schemes(){return <><Navbar/><main><Breadcrumb current="Schemes"/><PageHero eyebrow="Schemes" title="Learning backed by access." copy="Training and certification initiatives delivered by T2T Skill Foundation."/><section className="section"><div className="scheme-grid">{schemeList.map(([slug,item])=><Link href={`/schemes/${slug}`} className="scheme" key={slug}><div><span>{item.year}</span><b>{item.trainees} trainees</b></div><h2>{item.name}</h2><p>{item.summary}</p><footer>View programme details →</footer></Link>)}</div></section></main><Footer/></>}
