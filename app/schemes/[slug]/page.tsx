import {notFound} from "next/navigation";import Link from "next/link";import {Navbar,Footer,Breadcrumb,Placeholder} from "../../components/site";import {schemes} from "../../components/data";
export function generateStaticParams(){return Object.keys(schemes).map(slug=>({slug}))}export default async function SchemeDetail({params}:{params:Promise<{slug:string}>}){const {slug}=await params;const item=schemes[slug as keyof typeof schemes];if(!item)notFound();return <><Navbar/><main><Breadcrumb current={item.name}/><section className="detail-hero"><div><p className="eyebrow">Scheme details · {item.year}</p><h1>{item.name}</h1><p>{item.summary}</p><div className="detail-stats"><b>{item.trainees}<small>Trainees</small></b><b>{item.agency}<small>Sponsoring agency</small></b></div></div><Placeholder/></section><section className="section reading detail-copy"><p>{item.detail}</p><Link className="btn" href="/contact">Contact T2T →</Link></section></main><Footer/></>}
